{
  "name": "@threadlens/desktop-electron",
  "version": "0.2.2",
  "private": true,
  "description": "Electron shell for ThreadLens",
  "author": "ThreadLens Contributors",
  "main": "main.cjs",
  "scripts": {
    "dev": "node scripts/prepare-dev.cjs && electron .",
    "icon:build": "node scripts/build-app-icon.cjs",
    "stage": "node scripts/stage-desktop-assets.cjs",
    "build": "pnpm run icon:build && pnpm run stage && node --check main.cjs && node --check main-menu.cjs && node --check preload.cjs && node --check main/api-lifecycle.cjs && node --check main/ipc-handlers.cjs && node --check main/smoke-runtime.cjs && node --check main/window-runtime.cjs",
    "dist:dir": "pnpm run build && electron-builder --dir",
    "dist:mac": "pnpm run build && electron-builder --mac zip",
    "dist:win": "pnpm run build && electron-builder --win portable",
    "dist:linux": "pnpm run build && electron-builder --linux AppImage",
    "smoke:packaged": "node scripts/smoke-packaged.cjs",
    "test": "node --test main-menu.test.cjs ipc-validation.test.cjs package-config.test.cjs main/api-lifecycle.test.cjs main/window-runtime.test.cjs main/smoke-runtime.test.cjs",
    "lint": "node --check main.cjs && node --check main-menu.cjs && node --check preload.cjs && node --check main/api-lifecycle.cjs && node --check main/ipc-handlers.cjs && node --check main/smoke-runtime.cjs && node --check main/window-runtime.cjs"
  },
  "devDependencies": {
    "electron": "^41.0.2",
    "electron-builder": "^26.0.12",
    "png-to-ico": "^2.1.8"
  },
  "build": {
    "appId": "com.threadlens.desktop",
    "productName": "ThreadLens",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.cjs",
      "main/**/*",
      "main-menu.cjs",
      "ipc-validation.cjs",
      "preload.cjs",
      "app/**/*",
      "package.json"
    ],
    "asarUnpack": [
      "app/api/**/*"
    ],
    "mac": {
      "category": "public.app-category.developer-tools",
      "icon": "build/icon.icns",
      "target": [
        "zip"
      ]
    },
    "win": {
      "icon": "build/icon.ico",
      "target": [
        "portable"
      ]
    },
    "linux": {
      "category": "Utility",
      "icon": "build/icon.png",
      "target": [
        "AppImage"
      ]
    }
  }
}
