{
  "name": "@provider-surface/tui",
  "version": "0.1.0",
  "description": "Terminal workbench for Provider Observatory",
  "license": "MIT",
  "type": "module",
  "bin": {
    "provider-surface-tui": "dist/cli.js"
  },
  "scripts": {
    "dev": "tsx watch src/cli.tsx",
    "build": "pnpm --filter @provider-surface/shared-contracts build && tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "lint": "pnpm --filter @provider-surface/shared-contracts build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@provider-surface/shared-contracts": "workspace:*",
    "ink": "^6.8.0",
    "react": "^19.2.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^19.2.14",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
